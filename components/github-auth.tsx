'use client';

/**
 * GitHubAuth stub — GitHub authentication has been removed.
 * This component is kept as a no-op so any remaining references don't break.
 */
export function GitHubAuth() {
  return null;
}
